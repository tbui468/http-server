Spec for http 1.1, http 2, and http 3
    https://www.rfc-editor.org/rfc/rfc9110.html

Read these two:
    https://www.jmarshall.com/easy/cgi/
        this is related to forms, not request parameters (???)
    https://www.jmarshall.com/easy/http/

need to make a basic template engine
    www.thomasbui.net/igcse/15-marker
    www.thomasbui.net/igcse/15-marker/2034 - takes to a particular test/autocheck (notice the clean routes here)

Autocheck
    Add example(s) to description showing students _all_ possible paths
    write tests that cover examples (public tests)
    write tests that are hidden

Replacing byte_array.h with gkab_array.h
    Implement more function for arrays
        require the function when making the array:
            GKAB_MAKE_ARRAY(u8array, uint8_t, u8_cmp_func);

        Add a new field for a function pointer that compares two elements of type: (int)(*cmp)(elem_t *e1, elem_t *e2);
        Implement find_element function (uses cmp field)
        Implement find_subarray function (uses cmp field)
        Implement concat_inplace function (concatenates second array onto first array, modifying first array.  Second array remains unchanged)

    Use gkab_array u8array and create wrappers for custom usage
        make a wrapper that appends a string to u8array
        make a wrapper that appends a u8array to u8array
        gkab_array should have a new function: find subarray
            need to bring over find also

    Replace all calls to byte_array with custom array made with GKAB_MAKE_ARRAY

change blog.service to use the executable in http project
    1. modify the blog.service file to point to correct executable
    2. start service

Use python_course project code as a reference, and build 
    an autocheck system for 15 point questions
    indentation
    custom user input
    give example outputs for each test question

    Two columns:
        buttons
        editor  description (including examples and results of autocheck)

Add templates for dynamic html generation
    What's the API we should use?
        rather than http_write_file, it should be http_write_template
        http_write_template(struct http_message *response, struct http_message *request, struct http_template_data *data) {
        }

        struct http_template_data {
            struct k
        }; 
        
    put in some test data for blog posts
    will need to parse parameters in url, ie http://localhost:8888/post?pid=2 
        and pass them to handlers (can put them in struct message *request)

General
    should probably make _all_ functions prepended with prefix http_ (including string, arena, byte_array, net)
        these structs are used internally with http module, gwl
    
    don't overuse mystring for everything - many things (such as port numbers) can remain const char*
        just use this when we need to store data in a struct somewhere
    
    test.sh no longer works since we are using routes now

    When browser connects to server for first time, 3 distinct connections are made (what's going on here?)

Router
    currently hard-coded to 16 routes max (excluding default route)
        should make this heap allocated so that it can get larger (will need to implement arena properly for this)
        just use arena from grci or other projects

Add postgres driver
    use libpq client library to connect

Add sessions
